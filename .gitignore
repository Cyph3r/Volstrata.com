# Copyright 2026 Volstrata.com - https://volstrata.com

# Secrets — only .env.example is committed
.env

# Python
.venv/
__pycache__/
*.pyc

# JavaScript
node_modules/

# Drift-check scratch space
.drift-tmp/

# OS
.DS_Store

# MCP client configs — only the templates in examples/mcp/ are committed.
# A config you fill in carries a live key, so the filled-in names are ignored
# at every level. `mcp.json.example` is a different filename and stays tracked.
mcp.json
.mcp.json
